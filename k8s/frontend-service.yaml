apiVersion: v1
kind: Service
metadata:
  name: ares-frontend-service
  namespace: devops-assignment
spec:
  selector:
    app: ares-frontend
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30000
  type: NodePort
